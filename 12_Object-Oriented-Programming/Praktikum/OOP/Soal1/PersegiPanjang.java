package OOP.Soal1;

public class PersegiPanjang extends BangunDatar{
    float panjang = 8;
    float lebar = 7;

    static float luas = (float)(panjang*lebar);
    static float keliling =  (float)(2 * (panjang + lebar));

}
